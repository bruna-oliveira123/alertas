name: Alerta Discord

on:
  push:
    branches: [ "main" ]

jobs:
  alertar:
    runs-on: ubuntu-latest
    steps:
      - name: Notificando o Discord
        uses: appleboy/discord-action@master
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          message: "Commit realizado com sucesso por ${{ github.actor }}!"
